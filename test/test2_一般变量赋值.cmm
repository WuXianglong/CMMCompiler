/**
 * 测试 一般变量赋值
 * 
 * 测试 一般变量赋值 主要是：
 * 	1.声明时赋值
 * 	2.算术运算赋值
 * 	3.read赋值
 * 	4.变量传递，即把一个变量赋值给另外一个变量
 * 	  涉及到类型转换问题
 *
 * 2010-12-19
 * 吴文苑
 */

//read赋值
real r;
read(r);
write(r);

//声明时赋值
int aa = 23,ab = (-4);
real b ,c =2.55;

//算术运算赋值
int a = 15 - 3 * ( 150 / 3 / 10);
if(a == 0)
	write(1);
else
	write(0);

//变量传递
real r2 = 5;
int a2 = 3;
r2 = a2;
if(r2 == 3)
	write(1);
else
	write(0);
r2 = a2 ;
write(r2);

/*output:
 *233		#输入的数字
 *233.0
 *1
 *1
 *3.0
 *///:~