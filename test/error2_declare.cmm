/** 
 * 变量声明测试
 *
 * 测试变量声明是否合法 主要是：
 * 	1.标识符是否满足 用例规范(以字母、
 *    数字、下划线组成，只能以字母开头
 *    ，且不能以下划线结束)
 *  2.在相同作用域内不允许出现同名变量
 *
 * 2010-12-19
 * 吴文苑
 */

//声明变量i
int i=0;
while(i<6){
	write(i);
	i=i+1;
}

//相同作用域内定义同名变量
int i=10;
write(i);

/*output
 *报错：变量i已被声明,请重命名该变量
 *///:~